<!DOCTYPE html>
<html>
  <head>
    <title>Binxtils Dummy</title>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav>
      <%= link_to "Cryptids", cryptids_path, id: "nav-cryptids", class: ("active" if current_page_active?(cryptids_path)) %>
      <%= link_to "Users", users_path, id: "nav-users", class: ("active" if current_page_active?(users_path)) %>
    </nav>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
